{% extends 'base.html.twig' %}

{% block container %}
    <div class="container">
        <div class="row">
            <div class="col-sm-8 col-md-offset-2 text-center">
                <h1>{{ 'cmb.challenge' | trans }}</h1>

                <p>{{ 'cmb.challenge.description' | trans }}</p>
            </div>

            <div class="col-sm-8 col-md-offset-2 text-center">
                {{ brawl.attacker.username }} VS {{ brawl.defender.username }}
            </div>
        </div>
    </div>
{% endblock %}
