<!-- Fixed navbar -->
<nav class="navbar navbar-default navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar"
                    aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ path('homepage') }}">{{ 'cmb.website.name' | trans }}</a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
            {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                <ul class="nav navbar-nav">
                    <li><a href="{{ path('user_default_profile') }}">{{ 'cmb.profile' | trans }}</a></li>
                    <li><a href="{{ path('app_brawl_opponents') }}">{{ 'cmb.list.opponents' | trans }}</a></li>
                    <li><a href="{{ path('homepage') }}">{{ 'cmb.dummy_link' | trans }}</a></li>
                </ul>

                <ul class="nav navbar-nav navbar-right">
                    <li><a href="{{ path('homepage') }}">{{ 'cmb.dummy_link' | trans }}</a></li>
                    <li>
                        <a href="{{ path('fos_user_security_logout') }}">
                            {{ 'layout.logout'|trans({}, 'FOSUserBundle') }}
                        </a>
                    </li>
                </ul>
                {% else %}
                    <ul class="nav navbar-nav navbar-right">
                        <li><a href="{{ path('hwi_oauth_connect') }}">{{ 'cmb.login_or_register'|trans() }}</a></li>
                    </ul>

            {% endif %}
        </div>
    </div>
</nav>