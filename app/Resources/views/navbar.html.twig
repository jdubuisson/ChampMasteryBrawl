<!-- Fixed navbar -->
<nav class="navbar navbar-default navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar"
                    aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ path('homepage') }}">{{ 'cmb.website.name' | trans }}</a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
            {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                <ul class="nav navbar-nav">
                    <li><a href="{{ path('user_default_profile') }}">{{ 'cmb.profile' | trans }}</a></li>
                    <li><a href="{{ path('app_brawl_opponents') }}">{{ 'cmb.list.opponents' | trans }}</a></li>
                    <li><a href="{{ path('app_brawl_listassaults') }}">{{ 'cmb.assaults' | trans }}</a></li>
                    <li><a href="{{ path('app_brawl_listdefenses') }}">{{ 'cmb.defenses' | trans }}</a></li>
                </ul>

                <ul class="nav navbar-nav navbar-right">

                </ul>
            {% endif %}
            <ul class="nav navbar-nav navbar-right">
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button"
                       aria-expanded="true">{{ 'cmb.language.switch'|trans() }} <span class="caret"></span></a>
                    <ul class="dropdown-menu" role="menu">
                        <li>
                            <a href="{{ path(app.request.get('_route'), app.request.get('_route_params')|merge({'_locale': 'fr'})) }}">Français</a>
                        </li>
                        <li>
                            <a href="{{ path(app.request.get('_route'), app.request.get('_route_params')|merge({'_locale': 'en'})) }}">English</a>
                        </li>
                    </ul>
                </li>
                <li>
                    <a href="{{ path('about') }}">{{ 'cmb.about' | trans }}</a></li>
                {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                    <li>
                        <a href="{{ path('fos_user_security_logout') }}">
                            {{ 'layout.logout'|trans({}, 'FOSUserBundle') }}
                        </a>
                    </li>
                {% else %}
                    <li><a href="{{ path('hwi_oauth_connect') }}">{{ 'cmb.login_or_register'|trans() }}</a></li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>